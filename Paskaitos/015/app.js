// Primitive. Only one value
const five = 5;

//Object
const animal = {
  name: `Lion`,
  age: 3,
};
console.log(animal);
animal.type = `Mean`;
animal.age = 4;
console.log(animal);
console.log(animal.name);
